<div align="center">
  <p>
    <img src="../README.assets/android.png">
  </p>
  <br>
  <h2>Basic</h2>
  <p>용어 관련 내용 정리</p>
  <br>
  <br>
</div>


## 🔥 네트워크 용어

### HTTP

- HTTP (Hyper Text Transfer Protocol)

  : 서버와 클라이언트가 서로 데이터를 주고받기 위해 사용되는 통신 규약

### HTTP 상태 코드

- HTTP 상태 코드

  : 클라이언트가 보낸 HTTP 요청이 성공했는지 실패했는지를 서버에서 알려주는 숫자 코드

- 3자리 숫자로 이루어져 있으며, 100~500 번대 까지 존재한다

  - 1xx(정보) : 요청이 수신되어 처리중
  - 2xx(성공) : 요청 정상 처리
  - 3xx(리다이렉션) : 요청을 완료하려면 추가 조치 필요
  - 4xx : 클라이언트 오류
  - 5xx : 서버 오류

- 400번대 차이

  - 400 (Bad Request) : 클라이언트가 잘못된 요청을 보냄을 의미
  - 401 (Unauthorized) : 요청자가 인증되지 않음
  - 403 (Forbidden) : 요청자가 승인되지 않음

### HTTP 메서드 종류

- HTTP 메서드

  : 클라이언트와 서버 사이에 이루어지는 요청(Request)과 응답(Response) 데이터를 전송하는 방식

- 주요 메서드

  - GET : 데이터 조회
  - POST : 데이터 등록(입력)
  - PUT : 데이터 전체 변경
  - PATCH : 데이터 부분 변경
  - DELETE : 데이터 삭제

### Retrofit

- HTTP API를 인터페이스로 선언해 호출하는 네트워크 라이브러리
- 어노테이션 기반으로 URL, 메서드, 파라미터 정의
- OkHttp를 내부 네트워크 엔진으로 사용

### Retrofit Interface

- API 엔드포인트를 함수로 정의
- 일시 중단 함수로 코루틴 기반 비동기 처리
- 반환 타입으로 도메인 모델 직접 사용

### HTTP 메서드 어노테이션

- @GET : 리소스 조회
- @POST : 리소스 생성
- @PUT : 리소스 전체 수정
- @PATCH : 리소스 부분 수정
- @DELETE : 리소스 삭제

### 요청 파라미터 어노테이션

- @Path : URL 경로 변수 (/boards/{id})
- @Query : 쿼리 파라미터 (?lastId=10)
- @Body : JSON 요청 본문
- @Part : Multipart 요청의 개별 파트

### Multipart Request

- 이미지/파일 업로드용 요청 방식
- @Multipart + MultipartBody.Part 사용
- JSON + 이미지 혼합 전송 가능

### BaseResponse 패턴

- 서버 응답을 공통 래퍼로 감싸는 구조
- 성공/실패, 메시지, 실제 데이터 일관성 있게 처리
- 에러 핸들링 및 공통 로직 분리 용이

### OkHttpClient

- 실제 HTTP 통신을 담당하는 네트워크 클라이언트
- 타임아웃, 인터셉터, 인증 방식 설정 가능
- Retrofit에 주입되어 사용됨

### OkHttp Interceptor

- 요청/응답을 가로채는 미들웨어
- 헤더 추가, 토큰 주입, 로깅, 요청 변조 처리
- 네트워크 호출 전 단계에서 동작

### AuthInterceptor

- 모든 요청에 Authorization 헤더 자동 추가
- 액세스 토큰 관리 로직 분리
- 인증 관련 관심사 단일 책임

### Authenticator

- 401 응답 발생 시 자동으로 토큰 재발급 처리
- 실패한 요청을 새 토큰으로 재시도
- Interceptor와 달리 응답 이후 동작

### Logging Interceptor

- 요청/응답 로그 출력 (디버깅 용도)
- BODY 레벨에서 헤더 + 바디 전체 확인 가능
- 릴리즈 빌드에서는 비활성화 권장?

### Kotlin Serialization

- JSON ↔ Kotlin 객체 직렬화/역직렬화 라이브러리
- 컴파일 타임 안전성 제공
- ignoreUnknownKeys로 서버 필드 변경에 유연 대응

### Converter Factory

- Retrofit에서 데이터 변환을 담당
- JSON → DTO, DTO → JSON 자동 처리
- Kotlin Serialization을 Converter로 사용

### Timeout 설정

- connect / read / write timeout 설정
- 네트워크 지연 또는 서버 무응답 방어
- 사용자 경험 보호 목적

### Refresh 전용 OkHttp / Retrofit

- 토큰 재발급 API 전용
- 순환 호출 및 인터셉터 충돌 방지
- 인증 로직 안정성 확보

### Hilt + NetworkModule

- 네트워크 객체를 싱글톤으로 관리
- OkHttp, Retrofit, API 의존성 자동 주입
- 테스트 및 확장성 향상

### Qualifier

- 동일 타입 객체를 구분하기 위한 식별자
- Refresh용 / 일반용 Retrofit 분리
- 의존성 충돌 방지

### suspend + Coroutine

- 네트워크 요청을 비동기로 처리
- 메인 스레드 블로킹 방지
- 구조화된 동시성 기반 안전한 실행



